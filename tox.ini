[tox]
envlist = ruff, py

[testenv]
deps =
    pytest
commands =
    pytest -v tests/ []

[testenv:reformat]
skip_install = True
deps =
    ruff
commands =
    ruff format
    ruff check --fix

[testenv:ruff]
skip_install = True
deps =
    ruff
commands =
    ruff check
    ruff format --check
